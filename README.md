# Real Estate Frontend 🏢

Современный фронтенд для платформы покупки недвижимости в Бишкеке.

## Tech Stack

- ⚛️ **React 18** - UI библиотека
- 📘 **TypeScript** - типобезопасность
- ⚡ **Vite** - быстрая сборка
- 🎨 **TailwindCSS** - стилизация  
- 🛣️ **React Router** - навигация
- 🔗 **Axios** - HTTP клиент
- 🐻 **Zustand** - state management

## Начало работы

### Установка зависимостей

```bash
npm install
```

### Запуск dev сервера

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:3000`

### Сборка для production

```bash
npm run build
```

### Preview production build

```bash
npm run preview
```

## Структура проекта

```
src/
├── components/         # Переиспользуемые компоненты
│   ├── layout/        # Layout компоненты (Header, Footer)
│   └── ApartmentCard.tsx
├── pages/             # Страницы (роуты)
│   ├── HomePage.tsx
│   ├── MarketplacePage.tsx
│   ├── LoginPage.tsx
│   ├── RegisterPage.tsx
│   └── ProfilePage.tsx
├── store/             # Zustand stores
│   └── authStore.ts
├── lib/               # Утилиты и библиотеки
│   └── api.ts         # Axios инстанс
├── types/             # TypeScript типы
│   └── index.ts
├── App.tsx            # Root компонент
├── main.tsx           # Entry point
└── index.css          # Global styles
```

## API Integration

Backend API должен быть запущен на `http://localhost:8080`.

Vite автоматически проксирует запросы с `/api` на backend.

### Endpoints

- `POST /api/auth/login` - Вход
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/send-otp` - Отправка OTP
- `POST /api/auth/verify-otp` - Проверка OTP
- `GET /api/marketplace/apartments` - Список квартир
- `GET /api/bookings/my` - Мои бронирования

## Features

### Реализовано ✅

- 🏠 **Главная страница** с hero и features
- 🏬 **Marketplace** - каталог квартир с фильтрами
- 🔐 **Регистрация** с OTP верификацией (3 шага)
- 🔑 **Авторизация** по телефону + пароль
- 👤 **Личный кабинет** с бронированиями
- 📱 **Responsive дизайн** для всех устройств
- 🎨 **Premium UI** с желто-золотыми акцентами

### Планируется 🚧

- 🏢 Детальная страница квартиры
- 📋 Бронирование квартиры
- 📄 Просмотр договоров
- 💳 История платежей
- 🔔 Уведомления

## Environment Variables

Создайте `.env` файл в корне проекта:

```env
VITE_API_URL=http://localhost:8080/api
```

## Scripts

- `npm run dev` - Запуск dev сервера
- `npm run build` - Production сборка
- `npm run preview` - Preview production
- `npm run lint` - Проверка кода ESLint

## Design

Дизайн основан на Figma макетах для рынка Кыргызстана:
- Валюта: KGS (сом)
- Язык: Русский
- Акценты: Желтый/золотой
- Целевой город: Бишкек

## Browser Support

- Chrome (latest)
- Firefox (latest)
- Safari (latest)
- Edge (latest)

---

**Developed with ❤️ for Bishkek real estate market**
